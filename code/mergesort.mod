;;; -*- Mode: Scheme; -*-

(define-module mergesort
  (exports
   take
   drop
   merge
   merge-sort
   merge-sort-traced
   insertion-sort
   insertion-sort-traced
   run-merge-sort-tests
   run-insertion-sort-tests)
  (requires assert))

;; eof
