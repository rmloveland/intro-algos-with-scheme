;;; -*- Mode: Scheme -*-

(define-module mergesort
  (exports
   merge
   merge-sort
   merge-sort-traced
   insertion-sort
   insertion-sort-traced
   run-merge-sort-tests
   run-insertion-sort-tests))

;; eof
