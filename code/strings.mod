;;; -*- Mode: Scheme -*-

(define-module strings
  (exports
   string-split
   string-trim
   string-tokenize
   pregexp-replace
   pregexp-replace*
   pregexp-quote
   *pregexp-comment-char*)
  (requires pregexp))

;; eof
