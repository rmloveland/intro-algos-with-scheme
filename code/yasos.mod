;; -*- Mode: Scheme -*-

(define-module yasos
  (exports
   define-operation
   define-predicate
   object
   object-with-ancestors
   operate-as
   make-instance
   instance?)
  (requires format))

;; eof

