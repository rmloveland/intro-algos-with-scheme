;;; -*- mode: scheme -*-

(define-module utils
  (exports

   ;; Random things
   random-integer
   random-char
   random-string

   ;; Lists
   make-list
   make-list*
   atom?
   alist?
   first
   second
   third
   rest
   flatten
   mapconcat
   filter
   seq
   take
   drop

   ;; Vectors
   vector-swap!))

;; eof
