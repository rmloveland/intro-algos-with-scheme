;;; -*- Mode: Scheme -*-

(define-module pregexp
  (exports
   pregexp
   pregexp-match
   pregexp-match-positions
   pregexp-split
   pregexp-replace
   pregexp-replace*
   pregexp-quote
   *pregexp-comment-char*))

;; eof
